import React from "react";
import "./css.scss";

const Loading: React.FC = () => {
  const componentName = "Loading";

  return <div className={`${componentName}Content`}></div>;
};

export default Loading;
